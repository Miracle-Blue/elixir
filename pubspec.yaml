name: elixir
description: "A new Flutter package project."
version: 0.1.1

homepage: https://github.com/Miracle-Blue/elixir

environment:
  sdk: ^3.9.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
